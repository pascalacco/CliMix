before_script:
  - ls
  - whoami
  - cat /var/www/climix_test/flaskapp/constantes.py
  - ls /var/www/climix_test/flaskapp
deploy_stage:
    stage: deploy
    environment: Staging
    script:
      - pwd
      - rm -rf .git/
      - rm flaskapp/static/logo.png
      - mv flaskapp/static/logoTest.png flaskapp/static/logo.png
      - cp -r . /var/www/climix-test/
deploy_production:
  stage: deploy
  environment: Production
  only:
    - tags
  script:
      - pwd
      - rm -rf .git/
      - cp -r . /var/www/climix/

